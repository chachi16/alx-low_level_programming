#include "main.h"
/**
 *_isupper - checks upper case and lower case letters
 *@c:character to be checked
 *return: 1 and 0
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
